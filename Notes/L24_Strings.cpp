#include <iostream>
using namespace std;

int main() {
    // Initialise a string
    string str;
    // Take input ( last character is always null character )
    cin >> str;
    // Display String
    cout << str;
    // Get the Length of String
    cout << str.length();
}