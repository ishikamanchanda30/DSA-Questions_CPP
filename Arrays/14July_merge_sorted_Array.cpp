#include <iostream>
#include <vector>
using namespace std;


void MergeSorted(vector <int> v, vector <int> v2, int n, int m) {
    vector <int< v3 (m+n);
    for () {

    }


}

int main() {
    vector<int> v(9);
    int n = v.size();
    v.push_back(11);
    v.push_back(22);
    v.push_back(33);
    v.push_back(0);
    v.push_back(0);
    v.push_back(0);
    
    vector<int> v2(3);
    v2.push_back(55);
    v2.push_back(66);
    v2.push_back(77);

    for (int i = 0; i < ans.size(); i++) {
        cout << ans[i] << " ";
    }

}